{
  "buildCommand": "cd client && pnpm install && pnpm build",
  "outputDirectory": "client/public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

